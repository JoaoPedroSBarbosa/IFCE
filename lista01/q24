#include <stdio.h>

int main(){
  
  int a, n, valor;

  puts("Digite um valor:");
  scanf("%d",&a);

  puts("Digite o expoente:");
  scanf("%d",&n);

  valor = a * 2 << n - 1;
  
  printf("O valor agora é: %d", valor);
  
  return 0;
  
}